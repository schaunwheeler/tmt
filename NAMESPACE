# NAMESPACE for TMT package

# Export all names
# exportPattern("^\\.(define|extend)")
export(SetEnvVarW)
export(AspellCheck)
export(CompleteStem)

# Import all packages listed as Imports or Depends
import(
  tm
)
